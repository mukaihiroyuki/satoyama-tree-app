# Code Quality Report — 2026-04-10

Scope: `src/` (excludes `node_modules`, `.next`, `.git`)
Previous report: `docs/quality-reports/2026-04-03.md`

---

## Check 1: console.log Remnants

### Debug leftover (1 issue) — **PERSISTS from 2026-04-03**

| File | Line | Description |
|------|------|-------------|
| `src/hooks/useTrees.ts` | 29 | `console.log('[useTrees] loaded: ...')` — debug trace, not an error handler |

### Note on console.error

All other `console.error` calls (~50 instances) are in `catch` blocks or Supabase error handlers with descriptive messages. These appear intentional and are not flagged.

---

## Check 2: TODO / FIXME Comments

**No issues found.** No TODO, FIXME, HACK, or XXX comments exist in the codebase.

---

## Check 3: Unused Imports

### NEW since 2026-04-03 (1 issue)

| File | Line | Description |
|------|------|-------------|
| `src/app/reservation-scan/page.tsx` | 3 | `import React, { ... }` — the `React` default is imported but never referenced as `React.*` anywhere in the file. Named hooks (`useState`, `useEffect`, `useRef`, `useCallback`) are used correctly. |

---

## Check 4: `any` Type Usage

All 6 instances carry `// eslint-disable-next-line @typescript-eslint/no-explicit-any` comments, unchanged from the previous report.

| File | Line | Usage | Notes |
|------|------|-------|-------|
| `src/lib/tree-repository.ts` | 31 | `let allData: any[]` | Supabase pagination buffer in `getAllTrees` — could use the inferred DB row type |
| `src/lib/tree-repository.ts` | 80 | `let allData: any[]` | Same pattern in `refreshTrees` — same fix applies |
| `src/lib/tree-repository.ts` | 649 | `function flattenShippedAt(rows: any[])` | Complex nested JOIN shape — type the input row explicitly |
| `src/app/scan/page.tsx` | 94 | `(window as any).BarcodeDetector` | Non-standard browser API — unavoidable |
| `src/app/scan/page.tsx` | 98 | `let detector: any` | Instance type depends on BarcodeDetector availability — acceptable |
| `src/app/scan/page.tsx` | 100 | `let jsQR: any` | Dynamically imported module — acceptable |

The two `tree-repository.ts` pagination buffers (lines 31, 80) remain the most addressable.

---

## Check 5: Unhandled Async Calls

### 5a. Supabase queries missing `error` destructuring (14 issues)

These queries destructure only `data`, silently ignoring Supabase errors. 13 persist from the previous report; 1 is new.

| File | Line | Pattern | Status |
|------|------|---------|--------|
| `src/app/clients/page.tsx` | 33 | `const { data } = await supabase.from('clients')...` — initial load | Persists |
| `src/app/clients/page.tsx` | 40 | `const { data } = await supabase.from('clients')...` — reload after save | Persists |
| `src/app/shipments/page.tsx` | 35 | `const { data } = await supabase.from('shipments')...` | Persists |
| `src/app/species/page.tsx` | 32 | `const { data } = await supabase.from('species_master')...` — load | Persists |
| `src/app/species/page.tsx` | 42 | `const { data } = await supabase.from('species_master')...` — reload | Persists |
| `src/app/estimates/[id]/page.tsx` | 246 | `const { data } = await supabase.from('trees')...` — tree search | Persists |
| `src/app/estimates/[id]/page.tsx` | 346 | `const { data: shipments } = await supabase.from('shipments')...` | Persists |
| `src/app/shipments/[id]/page.tsx` | 169 | `const { data } = await supabase.from('trees')...` — tree search | Persists |
| `src/app/reservation-scan/page.tsx` | 121 | `const { data } = await supabase.from('clients')...` | Persists |
| `src/components/ShipmentDialog.tsx` | 38 | `const { data } = await supabase.from('clients')...` | Persists |
| `src/components/ReservationDialog.tsx` | 34 | `const { data } = await supabase.from('clients')...` | Persists |
| `src/components/EstimateDialog.tsx` | 37 | `const { data } = await supabase.from('clients')...` | Persists |
| `src/components/EstimateDialog.tsx` | 76 | `const { data } = await supabase.from('estimates')...` — estimate number | Persists |
| `src/components/ActivityLogList.tsx` | 42 | `const { data } = await supabase.from('activity_logs')...` | **NEW** |

### 5b. Promise.all with incomplete error checking (1 issue) — **PERSISTS from 2026-04-03**

| File | Lines | Description |
|------|-------|-------------|
| `src/app/page.tsx` | 12–18 | `Promise.all` of 5 Supabase count queries. Only `totalRes.error` is checked (line 20). `inStockRes`, `reservedRes`, `shippedRes`, `disabledRes` errors are silently ignored; if any fail, their `.count` will be `null` and fall back to `0` with no log. |

### 5c. Fire-and-forget mutations — no error capture (6 issues)

These `await` calls discard the entire Supabase result; errors from the DB are invisible.

| File | Line | Description |
|------|------|-------------|
| `src/app/clients/page.tsx` | 253 | `await supabase.from('clients').update({ portal_show_price: ... })` — inline `onChange` handler, no error feedback to user |
| `src/app/clients/page.tsx` | 272 | `await supabase.from('clients').update({ portal_password: ... })` — inline `onBlur` handler, no error feedback to user |
| `src/app/trees/page.tsx` | 320 | `await supabase.from('shipments').delete()` — cleanup delete inside loop, result discarded |
| `src/app/shipments/[id]/page.tsx` | 257 | `await supabase.from('shipments').delete()` — cleanup after bulk cancel, result discarded |
| `src/app/shipments/[id]/page.tsx` | 441 | `await supabase.from('shipments').delete()` — cleanup after individual cancel, result discarded |
| `src/lib/activity-log.ts` | 56 | `await supabase.from('activity_logs').insert(rows)` — result never captured; the surrounding `try/catch` only catches thrown exceptions, not Supabase-level errors |

---

## Check 6: Hardcoded Secrets

**No API keys, tokens, or passwords found.**

As noted in the previous report, `src/lib/constants.ts` contains sequential staff PINs (`"0001"`–`"0007"`) as plaintext. These are intentional per project design (internal-only, localStorage-based auth). No change.

---

## Summary

| Check | Issues | vs. 2026-04-03 |
|-------|--------|----------------|
| 1. console.log remnants | **1** | No change |
| 2. TODO/FIXME comments | **0** | No change |
| 3. Unused imports | **1** | +1 NEW (`React` in `reservation-scan/page.tsx`) |
| 4. `any` type usage | **6** (all suppressed) | No change |
| 5. Unhandled async errors | **21** | +7 vs. previous report (1 new missing-error + 6 fire-and-forget) |
| 6. Hardcoded secrets | **0** | No change |

**Total actionable issues: 23** (up from 15 last week)

### Recommended Priorities

1. **(High — new this week)** `src/app/clients/page.tsx:253,272` — inline mutation handlers silently fail. Add `const { error } = await ...` and show an alert on failure.
2. **(High — new this week)** `src/lib/activity-log.ts:56` — capture and log the Supabase result: `const { error } = await supabase.from(...).insert(rows); if (error) console.error(...)`.
3. **(Medium — persists 2 weeks)** Fix the 14 `const { data } = await supabase...` patterns — add `error` destructuring so failures surface in production logs.
4. **(Medium — persists 2 weeks)** Fix `src/app/page.tsx` Promise.all — check all 5 result errors, not just `totalRes.error`.
5. **(Medium — new this week)** `src/app/trees/page.tsx:320`, `src/app/shipments/[id]/page.tsx:257,441` — cleanup deletes should at minimum log errors: `const { error } = await ...; if (error) console.error(...)`.
6. **(Low — persists 2 weeks)** Remove `src/hooks/useTrees.ts:29` `console.log`.
7. **(Low — new this week)** Remove unused `React` default import from `src/app/reservation-scan/page.tsx:3`.
8. **(Low — persists 2 weeks)** Type the two `allData: any[]` pagination buffers in `tree-repository.ts` using the inferred Supabase row type.
