# Code Quality Report — 2026-04-03

Scope: `src/` (excludes `node_modules`, `.next`, `.git`)

---

## Check 1: console.log Remnants

### Debug leftover (1 issue)

| File | Line | Description |
|------|------|-------------|
| `src/hooks/useTrees.ts` | 29 | `console.log(\`[useTrees] loaded: ...\`)` — debug trace, not an error handler |

### Note on console.error

All other `console.error` calls (~50 instances) are in `catch` blocks or Supabase error handlers with descriptive messages. These appear to be intentional and are not flagged.

---

## Check 2: TODO / FIXME Comments

**No issues found.** No TODO, FIXME, HACK, or XXX comments exist in the codebase.

---

## Check 3: Unused Imports

**No issues found.** All imported symbols are used in their respective files.

---

## Check 4: `any` Type Usage

All 6 instances have `// eslint-disable-next-line @typescript-eslint/no-explicit-any` comments, indicating they are known/deliberate. Listed for awareness:

| File | Line | Usage | Notes |
|------|------|-------|-------|
| `src/lib/tree-repository.ts` | 31 | `let allData: any[]` | Supabase pagination buffer — could be typed as the DB row type |
| `src/lib/tree-repository.ts` | 80 | `let allData: any[]` | Same pattern in `refreshTrees` — same fix applies |
| `src/lib/tree-repository.ts` | 649 | `function flattenShippedAt(rows: any[])` | Complex nested shape from JOIN query — type the input row explicitly |
| `src/app/scan/page.tsx` | 94 | `(window as any).BarcodeDetector` | Non-standard browser API unavoidable |
| `src/app/scan/page.tsx` | 98 | `let detector: any` | Type depends on BarcodeDetector availability — acceptable |
| `src/app/scan/page.tsx` | 100 | `let jsQR: any` | Dynamic import with complex type — acceptable |

The two `tree-repository.ts` pagination buffers (lines 31, 80) are the most addressable: the Supabase select return type could replace `any[]`.

---

## Check 5: Unhandled Async Calls

### 5a. Supabase queries missing `error` destructuring (11 issues)

These queries destructure only `data`, silently ignoring Supabase errors:

| File | Line | Pattern |
|------|------|---------|
| `src/app/clients/page.tsx` | 33 | `const { data } = await supabase.from('clients')...` — load |
| `src/app/clients/page.tsx` | 40 | `const { data } = await supabase.from('clients')...` — reload after save |
| `src/app/shipments/page.tsx` | 35 | `const { data } = await supabase.from('shipments')...` |
| `src/app/species/page.tsx` | 32 | `const { data } = await supabase.from('species_master')...` — load |
| `src/app/species/page.tsx` | 42 | `const { data } = await supabase.from('species_master')...` — reload |
| `src/app/estimates/[id]/page.tsx` | 246 | `const { data } = await supabase.from('trees')...` — tree search |
| `src/app/estimates/[id]/page.tsx` | 346 | `const { data: shipments } = await supabase.from('shipments')...` |
| `src/app/shipments/[id]/page.tsx` | 169 | `const { data } = await supabase.from('trees')...` — tree search |
| `src/app/reservation-scan/page.tsx` | 121 | `const { data } = await supabase.from('clients')...` |
| `src/components/ShipmentDialog.tsx` | 38 | `const { data } = await supabase.from('clients')...` |
| `src/components/ReservationDialog.tsx` | 34 | `const { data } = await supabase.from('clients')...` |
| `src/components/EstimateDialog.tsx` | 37 | `const { data } = await supabase.from('clients')...` |
| `src/components/EstimateDialog.tsx` | 76 | `const { data } = await supabase.from('estimates')...` |

### 5b. Promise.all with incomplete error checking (1 issue)

| File | Line | Description |
|------|------|-------------|
| `src/app/page.tsx` | 12–30 | `Promise.all` of 5 Supabase queries. Only `totalRes.error` is checked (line 21). `inStockRes`, `reservedRes`, `shippedRes`, `disabledRes` errors are not verified before reading `.count`. |

---

## Check 6: Hardcoded Secrets

### No API keys, tokens, or passwords found.

One notable item that is not a traditional secret but warrants attention:

| File | Lines | Description |
|------|-------|-------------|
| `src/lib/constants.ts` | 11–17 | Staff PINs hardcoded as plaintext (`"0001"` – `"0007"`). Per `HOME_SYNC.md` these are intentional (localStorage-based, no-network auth). Risk is low given the app is internal, but sequential PINs are trivially guessable. |

No Supabase keys, JWT secrets, or other credentials were found in source files. Environment variables are used correctly via `NEXT_PUBLIC_SUPABASE_URL` and `NEXT_PUBLIC_SUPABASE_ANON_KEY`.

---

## Summary

| Check | Issues |
|-------|--------|
| 1. console.log remnants | **1** — `src/hooks/useTrees.ts:29` |
| 2. TODO/FIXME comments | **0** |
| 3. Unused imports | **0** |
| 4. `any` type usage | **6** (all suppressed with eslint-disable; 2 are addressable) |
| 5. Unhandled async errors | **14** (13 missing error destructuring + 1 incomplete Promise.all) |
| 6. Hardcoded secrets | **0** (sequential staff PINs noted but intentional) |

### Recommended Priorities

1. **(Medium)** Fix the 13 `const { data } = await supabase...` patterns — add `error` destructuring and minimal error logging to surface failures in production.
2. **(Medium)** Fix `src/app/page.tsx` Promise.all — check all 5 result errors, not just `totalRes.error`.
3. **(Low)** Remove `src/hooks/useTrees.ts:29` `console.log`.
4. **(Low)** Type the two `allData: any[]` pagination buffers in `tree-repository.ts` using the inferred Supabase row type.
